# CSS Audit - 2026-09-05

## Scope

- Parsed styles.css, styles.calendar.css, and styles.presenter-output.css.
- Inspected service, praise, scripture, calendar, and references at 390, 768,
  1024, and 1440 CSS pixels, including the expanded sidebar.
- Reviewed light/dark screenshots and section-editor controls.

## Fixes

- Replaced undefined color, motion, font, shadow, and icon tokens with existing
  design tokens or a standard monospace font stack. The always-on-top switch
  now has a visible thumb and a defined transition.
- Removed intrinsic minimum-width overflow from weekly/upcoming service cards.
- Made reference cards fit their container. Narrow cards put actions below
  the title; section headings and creation controls can wrap.
- Stacked the calendar heading and tabs on narrow viewports.
- Reorganized section-editor fields into rows that fit the panel, including
  its four action buttons.

## Verification

- CSS parsing and git diff whitespace checks passed.
- Module viewport matrix: no document/pane horizontal overflow or visible
  controls outside the detail pane after the fixes.
- smoke_css_controls.py: light/dark, 1440/800/390, passed.
- smoke_multiline_layout.py and smoke_preview_lifecycle.py passed.
- smoke_app.py passed on the final run, including page/console error checks.
  An earlier run failed in praise filter/comparison checks; both the old-CSS
  comparison run and the final modified-CSS run passed. The intermittent
  failure was not attributed to CSS without evidence.

Browser verification used Chromium. Safari compositing and every possible
user-data combination are outside the verified scope.
